name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # - name: Terraform Init
    #   run: |
    #     cd terraform
    #     terraform init

    # - name: Terraform Apply
    #   run: |
    #     cd terraform
    #     terraform apply -auto-approve

    # - name: Ansible Playbook
    #   run: |
    #     ansible-playbook -i inventory site.yml --ask-become-pass --ask-vault-pass
    #   env:
    #     ANSIBLE_VAULT_PASSWORD: "0"
    #     SSH_PRIVATE_KEY: "jya62xsLPGW3sp3lRqU4gD9AST1EcbFirV8x6006"
